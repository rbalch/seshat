"""NOOA agents: the model's four generation points.

Each agent is one class, with generation methods whose body is `...` and whose
docstring is the prompt (see AGENTS.md and `nooa/ellipsis_detection.py`). This
package holds one module per generation point; `verifier_author.py` is the
second (plan §2, T-07).
"""

from __future__ import annotations
